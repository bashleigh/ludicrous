import { Handler, APIGatewayEvent } from 'aws-lambda'

export const lambda: Handler<APIGatewayEvent> = (event, context) => {
  // TODO create container
  // TODO get controller providers using event.url

  
}
